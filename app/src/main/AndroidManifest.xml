<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthme">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".PostRegistro"></activity>
        <activity android:name=".Prev_Receta" />
        <activity android:name=".CrearReceta" />
        <activity android:name=".DetalleUsuario" />
        <activity android:name=".ListaUsuarios" />
        <activity android:name=".Home" />
        <activity android:name=".Info" />
        <activity android:name=".HealthMe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ElegirDieta" />
        <activity android:name=".Login" />
        <activity android:name=".Registro" />
        <activity android:name=".RegistroLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>